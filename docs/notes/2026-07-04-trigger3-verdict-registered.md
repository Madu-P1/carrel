# Trigger #3 is answered: the kernel is domain-general on anchored fabrications (registered 2026-07-04)

Status: VERDICT. This note exists because the answer to the council's revisit-
trigger #3 (blueprint 2026-07-02, "RUN THIS WEEK, cheapest test") was produced
the same day the council asked for it — `evals/reports/kernel-residue-2026-07-02.md`
— and then never registered in any strategy document. Two days later a
commissioned pro-thesis advocate searched the repo and reported the experiment
unrun; the billions plan (2026-07-04) repeated the error. The result existed the
whole time. This note is the registration, plus a fresh adversarial confirmation.

## The verdict

**The domain-agnostic kernel residue is NOT a trivial fraction of non-legal
fabrication catching. The "universal kernel" hypothesis survives its test; the
'family of domain products wearing a shared library' branch of trigger #3 does
NOT fire.**

Evidence, two independent batteries:

1. **The frozen parity corpus (2026-07-02, reproduced on main 2026-07-04):**
   15/15 (100%) catch on anchored non-legal fabrications across finance,
   consulting, medical, tax, operations, journalism. 0 false greens, 0 false
   accusations, 5/5 faithful confirmed, 5/5 honest refusal on documented gaps.
   `./.venv/bin/python -m evals.kernel_residue.run`

2. **Out-of-distribution adversarial rotation (2026-07-04, this session):**
   26 fresh attacks across 8 families the frozen corpus does not contain
   (paraphrase drift, multi-figure masking, representation equivalence,
   unit/currency swaps, quote evasion incl. stitching, cross-fact value
   laundering, anchor-free flips, superseded conflicts). **Honesty floor held
   26/26: 0 false greens, 0 false accusations.** OOD catch rate 9/15, every
   miss an honest refusal. Full report: `.claude/adversary/report-2026-07-04.md`.

## What this unblocks (the consequences that were sitting idle)

1. **Vulcan's recorded conditional is satisfied.** The blueprint gated even the
   kernel extraction on trigger #3 coming back positive. It is positive. The
   ADR-0014/0015 extraction + daemon work proceeds ungated.
2. **The expansion ladder's first rung is evidence-backed.** Tax (Circular 230
   citation-grounding) was argued kernel-shaped by analysis; the tax cases in
   both batteries (T1–T3 + threshold drifts) now show it empirically.
3. **The validation-demo line exists:** "attacked out-of-distribution across 8
   adversarial families, the engine held 26/26 — what it could not prove, it
   refused; it never guessed."

## The honest boundaries (unchanged by the pass)

- The kernel's domain is **claims with verbatim-checkable anchors**. The
  verbatim/semantic gap (call 02: "a liability clause without the word
  liability") is untouched by this result.
- OOD catch is 60%, not 100%: negation-flip guard, unit-mismatch comparator,
  and vocabulary-change paraphrase are the named catchable misses; all degrade
  to refusal, never to green.
- 5 P2 unnecessary refusals (scale words, date formats, written-out numbers,
  percent word, conflict-veto-on-faithful) are the definite-rate campaign's
  next food, per the mission lock's one-metric.

## Process lesson (cheap, worth keeping)

An experiment that is run but not REGISTERED does not exist for decision-making:
two councils and one advocate argued a question whose answer was on disk. Rule:
when a pre-registered trigger's experiment runs, the result gets a dated note in
`docs/notes/` that names the trigger and states which branch fired, same day.
