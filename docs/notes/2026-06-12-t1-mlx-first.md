# T1 local inference: MLX-first (note for future work)

When the Cachet T1 local discriminative tier lands (operator-gated, ADR-0012):
default to MLX (or Ollama's MLX backend, preview since 2026-03) over legacy
Ollama — on Apple Silicon MLX sustains ~230 tok/s vs Ollama's 20-40 and
degrades far less at long context (jarvis-upgrade 02-research §1.8, primary
benchmark). Refresh the stale local model set (qwen2.5:7b / llama3.1:8b /
deepseek-r1:8b, all 1-2 months old) at the same time. No action before T1
starts.
