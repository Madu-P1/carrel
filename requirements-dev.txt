ruff
mypy
# Test-only: reportlab generates the PDF fixtures consumed by
# test_docling_pdf_ingest.py. Not pulled into the app bundle.
reportlab>=4.0,<5.0
# Test-only: services/ingestion/memory_pressure.py reads psutil on the
# Linux / Windows / CARREL_FORCE_PSUTIL_MEMORY=1 path; macOS production
# uses the vm_stat + sysctl shellouts. Not pulled into the app bundle.
psutil>=5.9,<7.0
