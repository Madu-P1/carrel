"""Kernel-residue parity experiment (ADR-0015, council revisit-trigger #3).

Measures what the DOMAIN-AGNOSTIC residue of the deterministic verify kernel
(quote, money, percent, duration, magnitude, date -- no legal packs) catches on
realistic NON-LEGAL AI fabrications. The result sizes the kernel-extraction bet:
a trivial catch rate means the "universal kernel" is a family of domain products
wearing a shared library; a real catch rate means the extraction is buying a
genuinely portable primitive.
"""
