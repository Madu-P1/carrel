# Cachet standalone-product mode. Run with `vite --mode cachet` (dev) or
# `vite build --mode cachet`. Renders the standalone Cachet shell (verify +
# shelf, paper-first, no study chrome) instead of the full Carrel study app.
# Default (no mode) leaves the study app unchanged.
VITE_CACHET_ONLY=true
