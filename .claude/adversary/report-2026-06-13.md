# Cachet Adversary — first report, 2026-06-13

**Headline: the refusal engine HELD 15/15 adversarial attacks across 8 families. Zero false greens. Zero false accusations.** Run live against `verify_claim_against_clause` (deterministic, no network).

## Round 1 — obvious attacks (8/8 held)
near-miss money (x2), dropped digit, near-miss %, polarity flip (x2), governing-law lookalike, duration-unit swap → every one returned `parametric_contradiction`. 3/3 controls presented correctly.

## Round 2 — subtle classes (7/7 held honestly)
- spelled-out vs numeric ("five million" vs $1,050,000) → contradiction ✓
- party flip (indemnity direction) → not_found ✓ (refused, didn't false-green)
- defined-term alias the source never binds → not_found ✓
- off-by-one duration (31 vs 30 days) → contradiction ✓
- format noise ($1,050,000.00 == $1,050,000) → present ✓ (no false accusation)
- basis points == percent (50bps == 0.5%) → present ✓ (no false accusation)

## The one soft spot (HONEST direction — log, don't alarm)
`multi-value`: a verbatim-correct "$5,000" claim against a clause packing two fees ("$5,000 setup AND $40,000 annual") returns `multi_value_unverifiable` — it refuses a true claim. This is the **known residual T1 class** (role-aligned clause matching, gated behind validation per ADR-0008). It is NOT a crack: the engine errs toward refusal, never toward a false green. **Forge candidate, security-gated** (touches the truth surface) — queue only after T66, per the engine contract's operator-gated note.

## Validation-demo line (use verbatim)
"I built a red-team that attacks the verifier's honesty promise. 15 attacks, 8 families: it never once cleared a fabricated or altered claim, and never once falsely accused a clean one. Its only weakness is that it occasionally refuses a correct claim rather than risk a wrong 'verified.' That is the product working as designed — honest by construction."

## Coverage + next
Attacked: numerics, polarity, governing-law, duration, defined-term, party, multi-value, format-equivalence. NOT yet attacked: quote-alteration evasion (needs the quote_check path, services/legal/quote_check.py), citation-existence under a mismatched complete-scope manifest (the E2 hole Forge is fixing tonight — re-run this family after E2 ships to confirm the fix). Stale-fixture kill date: 2026-08-31.
